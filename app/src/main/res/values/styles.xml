<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/menu</item>
        <item name="colorAccent">@color/menu</item>
        <item name="colorControlNormal">@color/menu</item>
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlNormal">@color/menu</item>
        <item name="android:textColor">@color/menu</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--Controls the color of the hamburger menu and three dots on MainActivity-->
        <item name="colorControlNormal">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="AppTheme.AppBarOverlay.Login" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--Controls the color of the hamburger menu and three dots on MainActivity-->
        <item name="colorControlNormal">@color/menu</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/menu</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>


    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">
        <item name="android:textColor">@color/menu</item>
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <!--<item name="windowNoTitle">false</item>-->
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/menu</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/menu</item>
        <item name="android:textColor">@color/menu</item>
        <item name="colorControlNormal">@color/menu</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="AppTheme.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <!--<style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />-->

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

    <style name="NoShadowButton" parent="android:style/Widget.Button">
        <item name="android:stateListAnimator">@null</item>
        <item name="android:buttonStyle">@style/NoShadowButton</item>
        <item name="android:spotShadowAlpha">@null</item>
    </style>

</resources>
